# Ably API Key (for server-side token generation)
# Get this from https://ably.com/dashboard
VITE_ABLY_API_KEY=your-ably-api-key-here

# Or use environment-specific files:
# .env.local (git-ignored, for local development)
# .env.production (for deployment)
